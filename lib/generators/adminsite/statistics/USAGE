NAME
     statistics - generates a new statistic for a defined class

SYNOPSIS
     statistics

DESCRIPTION
     generates a new statistic for a defined class

EXAMPLE
     rails generate statistics [ClassName]

     This will generates a new statistic for a defined class