NAME
     importer - generates a new importer for a defined class

SYNOPSIS
     importer

DESCRIPTION
     generates a new importer for a defined class

EXAMPLE
     rails generate importer [ClassName]

     This will generates a new importer for the defined class to be used with a rake task