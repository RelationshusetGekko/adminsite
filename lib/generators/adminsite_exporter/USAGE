NAME
     exporter - generates a new exporter for a defined class

SYNOPSIS
     exporter

DESCRIPTION
     generates a new exporter for a defined class

EXAMPLE
     rails generate exporter [ClassName]

     This will generates a new exporter for the defined class to be used in Adminsite