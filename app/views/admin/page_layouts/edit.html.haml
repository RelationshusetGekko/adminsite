- content_for :header_left do
  Edit page layout “#{@page_layout.title}”
- content_for :header_right do
  = link_to_back 'Back', admin_page_layouts_path

= error_messages_for @page_layout

= form_for([:admin, @page_layout]) do |f|
  #page-edit
    = render :partial => 'form', :locals => { :f => f }
    = submit_tag "Update"
= render :partial => 'admin/shared/syntax_help'
= render :partial => 'admin/shared/page_assets_list'
.clear
